{
  "name": "one-estate-coffee",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "client",
    "server",
    "db"
  ],
  "scripts": {
    "dev": "concurrently -n server,client -c bgBlue,bgGreen \"npm run dev --workspace=server\" \"npm run dev --workspace=client\"",
    "dev:client": "npm run dev --workspace=client",
    "dev:server": "npm run dev --workspace=server",
    "migrate": "npm run migrate --workspace=db",
    "install:all": "npm install",
    "build": "npm run migrate --workspace=db || true && cd client && npm install --include=dev && npm run build",
    "start": "npm run start --workspace=server"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
